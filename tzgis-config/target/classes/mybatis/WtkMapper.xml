<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hm.tzgis.mapper.WtkMapper">

    <select id="queryWkBySbmc" resultType="map">
        select *
        from t_zb_wtk
        where wtlx = #{wtlx} and wtsbmc = #{sbmc}
    </select>


</mapper>